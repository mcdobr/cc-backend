cors:
  allowed:
    origins: ${CORS_ALLOWED_ORIGINS}
database:
  uri: ${DATABASE_URI}
  username: ${DATABASE_USER}
  password: ${DATABASE_PASSWORD}
keycloak:
  realm: demo
  auth-server-url: ${IDENTITY_PROVIDER_URI}/auth
identity_provider:
  uri: ${IDENTITY_PROVIDER_URI}
server:
    port: 8081
spring:
#  security:
#    oauth2:
#      resourceserver:
#        opaquetoken:
#          introspection-uri: https://www.googleapis.com/oauth2/v1/tokeninfo
      #    client-id: ${CC_OAUTH2_CLIENT_ID}
      #    client-secret: ${CC_OAUTH2_CLIENT_SECRET}
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
logging:
  level:
    org:
      springframework:
        security: DEBUG
        r2dbc: DEBUG